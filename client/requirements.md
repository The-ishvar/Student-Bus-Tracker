## Packages
date-fns | Formatting dates for bookings and messages
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
- Bilingual (English / Hindi) labels are used throughout the UI.
- AI features are simulated on the frontend as requested (randomized deterministic states based on IDs).
- Authentication uses a standard cookie-based or session-based flow assuming backend handles it via /api/auth/login and /api/auth/me.
